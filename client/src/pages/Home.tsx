import { ReactNode } from "react";
import { Footer, Header, Hero } from "../components";

const Home = (): ReactNode => {
    return <>
        <Header />
        <Hero />
        <Footer />
    </>
}

export default Home